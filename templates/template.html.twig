{% extends "base.html.twig" %}

{% block body %}

<header class="text-center">
  <h1> BTGeek</h1>
  <p> Construisons ensemble un monde de parpaings</p>
</header>

{% if is_granted('IS_AUTHENTICATED_FULLY') %}
<nav class="navbar navbar-expand-lg navbar-dark bg-info text-dark">
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarColor02">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="{{ path("project_index") }}">Mes projets</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{ path("project_new")}}">Nouveau projet</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#">Archive</a>
      </li>
    </ul>
  </div>
{% endif %}
{% if is_granted('IS_AUTHENTICATED_FULLY') %}
  {% if app.user %}
    <p class="mx-5">Bienvenue {{ app.user.username}} <p>
  {% endif %}
    <a href="{{ path("app_logout") }}" type="button" class="btn btn-warning ">Déconnection</a>
  {% else %}
    <a  href="{{ path("app_login") }}" type="button" class="btn btn-success m-3">Connection</a>
    <a  href="{{ path("app_register") }}" type="button" class="btn btn-warning m-3">S'inscrire</a>
{% endif %}
</nav>
<div class="container my-5">
{% block main %}{% endblock %}

<footer class="text-center">
  <p>&copy; BTGeek by Mathieu - November 2020</p>
{% endblock %}